<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">LOGO</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_article') }}">Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_article') }}">Article</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('newArticle') }}">Ajouter un article</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('modifier_profil') }}">Mon Profil</a>
                </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Plus</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Action</a>
                        {% if is_granted('ROLE_SUPERADMIN') == true %}
                        <a class="dropdown-item" href="{{ path('type') }}">Ajouter un type</a>
                        {% endif %}
                        <a class="dropdown-item" href="{{ path('utilisateurs') }}">Users</a>

                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Se deconnecter</a>
                    </div>
                    {% endif %}
                </li>
            </ul>
            <div class="d-flex">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_logout') }}">Deconnexion</a>
                    </li>
                    {% else %}
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('login') }}">Connexion</a>
                </li>


                {% endif %}
            </div>
        </div>
    </div>
</nav>